# ============================================================================
# normal people :: environment template
# Copy to .env locally; set these as Railway variables in production.
# ============================================================================

# --- Telegram ---------------------------------------------------------------
HUB_BOT_TOKEN=                  # From @BotFather
TIER1_CHANNEL_ID=               # -100xxxxxxxxxx  (the Library)
TIER2_GROUP_ID=                 # -100xxxxxxxxxx  (the Floor supergroup)
BOOTSTRAP_ADMIN_IDS=            # comma-separated Telegram user_ids (you)

# --- Database (Supabase) ----------------------------------------------------
# Use the "Connection pooling" URI from Supabase → Project Settings → Database
DATABASE_URL=postgresql://postgres:[PASSWORD]@[HOST]:6543/postgres

# --- Behavior tuning --------------------------------------------------------
INVITE_TTL_SECONDS=300          # single-use invite lifespan (5 min default)
CAPTCHA_TTL_SECONDS=120
STRIKE_MUTE_SECONDS=86400       # 24h read-only on Strike 1
STRIKE_DECAY_DAYS=90

# Mod review: set to a chat_id (e.g. private channel where you and other admins are members)
# Leave as 0 to DM each BOOTSTRAP_ADMIN_IDS user instead.
MOD_REVIEW_CHAT_ID=0

# Anti-lurk
HOME_TOPIC_ID=14              # thread_id of the #home topic in the floor
INTRO_GRACE_HOURS=24          # hours after joining to post intro or get kicked
SILENT_PING_DAYS=30           # days of silence before bot DMs 'still here?'
SILENT_DEMOTE_DAYS=7          # more days after ping before demoting to read-only
INACTIVITY_REMOVE_DAYS=60     # days demoted before full removal

LOG_LEVEL=INFO
